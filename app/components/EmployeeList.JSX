var React = require('react');

var EmployeeList = React.createClass({
  render: function() {
    return (
        <div>
          EmployeeList
        </div>
    );
  }
});

module.exports = EmployeeList;
