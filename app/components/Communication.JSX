var React = require('react');

var Communication = React.createClass({
  render: function() {
    return (
        <div>
          Communication
        </div>
    );
  }
});

module.exports = Communication;
